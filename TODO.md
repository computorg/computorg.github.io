- [x] Make .net setup + install computo cli tools skippable when not force=true
- [x] Fix quarto inspect missing include files - FIXED in workflow-automation with iterative approach
- [x] Mock repo test for missing include files - FIXED: Added parseMissingFileFromError and getAuthorsFromFrontmatter tests
- [x] Authors metadata extraction - FIXED: Added frontmatter fallback when quarto inspect doesn't provide authors
- [x] Check the official json schemas for quarto inspect - CONFIRMED: quarto inspect does NOT provide frontmatter metadata (title, authors, date) by design. The frontmatter fallback approach is correct.
- [x] Create a skill for CI check on github with gh cli - CREATED: gh-ci-check skill at workflow-automation/.agents/skills/gh-ci-check/
- [x] grab the official documentation for sitemap, generate it from quarto render - ALREADY WORKING: Quarto auto-generates sitemap.xml when site-url is configured in _quarto.yml
- [x] The issue with the site is that articles hosted individually in their own computorg repositories via GitHub Pages (they are also Quarto renders) and listed on the site are not properly indexed by Google Scholar. We need to review Google's official recommendations (for Scholar and sitemaps) and check if anything is missing from our side.
  - **FIXED**: Added `google-scholar: true` to all 11 published article repos that were missing it. Quarto auto-generates the required `<meta name="citation_*">` tags (Highwire Press format) when this flag is set.
  - **Root cause**: The computo-quarto-extension template already has `google-scholar: true`, but some articles (especially 2026) were published without it.
  - **Google Scholar requirements**: At minimum needs `citation_title`, `citation_author` (at least one), and `citation_publication_date` or `citation_year`. Quarto generates all of these from the `citation:` block in `_quarto.yml` when `google-scholar: true` is set.
  - **Verified**: All 25 published repos now have `google-scholar: true` in their `_quarto.yml`.

- [ ] Document Google Scholar indexing on the website
  - Add a page or section explaining how Computo ensures Google Scholar indexing
  - Mention the `google-scholar: true` flag and Highwire Press meta tags
  - Link to Google Scholar inclusion guidelines
  - Could be added to site/about.qmd or a new site/google-scholar.qmd

- [ ] Verify Google Scholar indexing status for all Computo articles
  - Use Google Scholar search to check which articles are indexed
  - Search for: `site:computo-journal.org` and `source:Computo`
  - Check for common issues: missing authors, wrong dates, missing PDF links
  - Create a report of indexed vs non-indexed articles
  - Tools: Google Scholar search, search.google.com/test-rich-results

- [ ] Résumé des commits de la semaine (19-26 juillet 2026) pour les éditeurs
  - **Automatisation** : Mise en place du CI/CD pour publier automatiquement les outils .NET de Computo sur GitHub Packages. Les outils de mise à jour des publications sont maintenant testés et déployés automatiquement. Des outlis supplémentaires (DOI, génération de README.md etc. par exemple pourront y être ajoutés)
  - **Site web** : Nettoyage du dépôt principal et correction de l'authentification pour l'installation des outils dans le CI.
  - **Indexation Google Scholar** : Ajout des méta-données requises dans les 11 articles publiés qui en manquaient. Tous les 25 articles ont maintenant les balises `<meta name="citation_*">` nécessaires pour l'indexation.
  - **Source officielle** : [Google Scholar Inclusion Guidelines](https://scholar.google.com/intl/en/scholar/inclusion.html) - requiert au minimum `citation_title`, `citation_author`, et `citation_publication_date` au format Highwire Press.
  - **Prochaines étapes** : Documentater sur le site l'indexation Google Scholar, et vérification du statut d'indexation des articles.
